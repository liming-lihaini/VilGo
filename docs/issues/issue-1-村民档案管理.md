# ISSUE-1-村民档案管理

## 概述

实现村民"一人一档"规范化管理，支持村民基础信息录入、编辑、删除；关联社保、养老、低保等保障类档案信息，记录保障状态及变动；支持销户登记（标记销户状态，不删除原始数据）；提供按身份证号、姓名、住址等多条件查询，支持人口统计及档案报表导出（Excel格式）；基础信息以身份证号为唯一标识，杜绝重复录入。

## 功能需求

### F-RES-001 新增村民档案

| 需求项 | 内容 |
|--------|------|
| 需求名称 | 新增村民档案 |
| 需求描述 | 工作人员录入村民基础信息，系统校验身份证号唯一性后入库 |
| 输入 | 姓名、身份证号、性别、出生日期、联系方式、家庭住址、婚姻状况、政治面貌、保障类型、人员类型、人员角色、备注 |
| ��验 | 身份证号全局唯一性 |
| 输出 | 创建成功提示，档案出现在列表中 |
| 异常处理 | 身份证号重复提示"已存在相关记录，请核对后重新录入" |

### F-RES-002 编辑村民档案

| 需求项 | 内容 |
|--------|------|
| 需求名称 | 编辑村民档案 |
| 需求描述 | 修改村民信息（身份证号不可修改） |
| 输入 | 档案ID、要修改的字段及新值 |
| 校验 | 档案存在 |
| 输出 | 编辑成功提示，档案信息更新 |
| 异常处理 | 档案不存在提示错误 |

### F-RES-003 删除村民档案

| 需求项 | 内容 |
|--------|------|
| 需求名称 | 删除村民档案 |
| 需求描述 | 软删除档案，标记deleted=1，保留历史数据 |
| 输入 | 删除确认、档案ID |
| 校验 | 档案存在且未被删除 |
| 输出 | 确认弹窗，成功后档案从列表移除 |
| 批量删除 | 支持批量删除，需确认提示 |

### F-RES-004 销户登记

| 需求项 | 内容 |
|--------|------|
| 需求名称 | 销户登记 |
| 需求描述 | 标记档案为销户状态，保留原始数据供查询 |
| 输入 | 档案ID |
| 校验 | 档案存在且状态为正常 |
| 输出 | 销户成功，档案标记显示销户状态 |
| 日志记录 | 操作人、操作时间、操作类型 |

### F-RES-005 村民档案查询

| 需求项 | 内容 |
|--------|------|
| 需求名称 | 村民档案查询 |
| 需求描述 | 支持多条件模糊查询（身份证号、姓名、住址） |
| 输入 | 查询条件（身份证号/姓名/住址/人员类型/保障类型/销户状态） |
| 校验 | 查询条件格式 |
| 输出 | 符合条件的档案列表 |
| 分页 | 支持分页查询 |
| 排序支持 | 名称、时间升序/降序 |

### F-RES-006 人口统计

| 需求项 | 内容 |
|--------|------|
| 需求名称 | 人口统计 |
| 需求描述 | 按人员类型、保障类型等条件统计人口数据 |
| 输入 | 统计条件 |
| 校验 | - |
| 输出 | 统计结果（总数、分类统计） |

### F-RES-007 Excel报表导出

| 需求项 | 内容 |
|--------|------|
| 需求名称 | Excel报表导出 |
| 需求描述 | 将档案列表导出为Excel格式 |
| 输入 | 导出条件（可选） |
| 校验 | 存在可导出的数据 |
| 输出 | Excel文件下载 |

## 技术方案

### 前端界面设计

前端功能菜单为人员档案管理，包含以下子页面：
人员列表【二级菜单】，包含新增、编辑、删除、销户、数据导入、导出等操作；
数据统计【二级菜单】，可选择统计条件，展示统计结果；

点击菜单在主页面右侧展示管理界面，自动配置菜单和路由规则

#### 页面结构

```
┌────────────────────────────────────────────────────────────┐
│  顶部工具栏: [新增] [导入] [导出] [筛选] [统计]              │
├─────────────────────────────────────────────────────────────┤
│  查询条件: [身份证号] [姓名] [住址] [人员类型▼] [保障类型▼]  │
├─────────────────────────────────────────────────────────────┤
│                    档案列表区                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  序号 │ 身份证号 │ 姓名 │ 性别 │ 住址 │ 类型 │ 状态 │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │  1    │ 320...  │ 张三 │ 男   │ XX村 │ 群众 │ 正常 │   │
│  │  2    │ 321...  │ 李四 │ 女   │ XX村 │ 党员 │ 销户 │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  分页: 共 100 条 | 第 1/10 页 | [上一页] [下一页]           │
└─────────────────────────────────────────────────────────────┘
```

#### 关键组件

| 组件 | 描述 | 事件 |
|------|------|------|
| `ResidentList` | 档案列表，表格形式展示 | `click`, `dblclick`, `select` |
| `ResidentForm` | 新增/编辑表单弹窗 | `confirm`, `cancel` |
| `SearchBar` | 查询条件栏 | `search`, `reset` |
| `Toolbar` | 顶部工具栏 | `click` |
| `StatModal` | 统计弹窗 | `close` |
| `ConfirmDialog` | 确认删除弹窗 | `confirm`, `cancel` |

#### 交互流程

**新增档案流程**:
```
点击"新增" → 弹出表单对话框 → 录入村民信息 → 点击确定
→ 调用POST /api/resident/create → 成功→刷新列表 → 失败→提示错误
```

**销户流程**:
```
选择档案 → 点击"销户" → 弹出确认对话框 → 点击确认
→ 调用PUT /api/resident/cancel → 刷新列表 → 档案标记销户状态
```

**批量删除流程**:
```
勾选多个档案 → 点击"删除" → 弹出批量确认对话框 → 点击确认
→ 调用DELETE /api/resident/delete → 刷新列表 → 档案移入回收站
```

#### 异常提示

| 场景 | 提示信息 |
|------|----------|
| 身份证号重复 | "已存在相关记录，请核对后重新录入" |
| 必填字段为空 | "请补充必填信息（标注*字段）" |
| 档案不存在 | "档案不存在或已删除" |
| 查询无结果 | "暂无相关记录，请调整查询条件后重试" |

### 数据结构

**村民档案表 (residents)**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO | 档案ID |
| id_card | VARCHAR(18) | UNIQUE, NOT NULL | 身份证号 |
| name | VARCHAR(50) | NOT NULL | 姓名 |
| gender | VARCHAR(10) | | 性别 |
| birth_date | DATE | | 出生日期 |
| phone | VARCHAR(20) | | 联系方式 |
| address | VARCHAR(200) | | 家庭住址 |
| marital_status | VARCHAR(20) | | 婚姻状况 |
| political_status | VARCHAR(20) | | 政治面貌 |
| photo | VARCHAR(500) | | 照片路径 |
| guarantee_type | VARCHAR(20) | | 保障类型 |
| household_status | VARCHAR(10) | DEFAULT '正常' | 销户状态 |
| is_head | TINYINT | DEFAULT 0 | 是否户主 |
| person_type | VARCHAR(20) | | 人员类型 |
| person_role | VARCHAR(20) | | 人员角色 |
| remark | TEXT | | 备注 |
| creator | VARCHAR(50) | | 录入人 |
| created_at | DATETIME | | 录入时间 |
| updated_at | DATETIME | | 更新时间 |
| deleted | TINYINT | DEFAULT 0 | 软删除 |

### 接口设计

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 新增档案 | POST | /api/resident/create | 新增村民档案 |
| 更新档案 | PUT | /api/resident/update | 更新村民档案 |
| 删除档案 | DELETE | /api/resident/delete/{id} | 删除档案（软删除） |
| 获取档案 | GET | /api/resident/get/{id} | 获取单条档案 |
| 查询列表 | POST | /api/resident/list | 条件查询列表 |
| 销户登记 | PUT | /api/resident/cancel/{id} | 销户登记 |
| 人口统计 | POST | /api/resident/statistics | 人口统计 |
| Excel导出 | POST | /api/resident/export | Excel导出 |

### 请求参数示例

```json
// 新增村民档案
{
  "idCard": "320105199001011234",
  "name": "张三",
  "gender": "男",
  "birthDate": "1990-01-01",
  "phone": "13812345678",
  "address": "XX村XX组",
  "maritalStatus": "已婚",
  "politicalStatus": "群众",
  "guaranteeType": "社保",
  "isHead": false,
  "personType": "群众",
  "personRole": "普通用户",
  "remark": ""
}

// 条件查询
{
  "idCard": "",
  "name": "",
  "address": "",
  "personType": "",
  "guaranteeType": "",
  "householdStatus": "正常",
  "deleted": 0,
  "pageNum": 1,
  "pageSize": 10
}
```

### 业务规则

1. 身份证号：18位，全局唯一标识，不可重复
2. 销户后档案保留历史数据，查询时可识别销户状态
3. 删除为软删除，标记deleted=1
4. 保障类型：无/社保/养老/低保/其他
5. 人员类型：群众/党员/预备党员/在读学生/军人/其他
6. 人员角色：管理员/普通用户（预留扩展）

## 验收条件

- [x] 新增档案成功，档案出现在列表中
- [x] 身份证号重复创建，提示"已存在相关记录"错误
- [x] 编辑档案成功，信息更新显示
- [x] 删除档案，弹窗确认后软删除
- [x] 销户登记，档案标记销户状态
- [x] 多条件查询，返回符合条件的列表
- [x] 人口统计，按条件正确统计
- [x] Excel导出，格式正确可下载
- [x] 分页查询，翻页正常
- [x] 批量删除，需确认提示

## 依赖

- 数据库初始化 (residents表)
- 回收站模块（删除时调用）
- 操作日志模块

## 状态

- [ ] 待开发
- [ ] 开发中
- [ ] 待测试
- [ ] 完成